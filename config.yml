results_dir : 'candogan_results'
experiment_name : 'hart_mas_2006'
latex_plot_potential : False

# Potentialness in sealed bid first price
plot_auction_potentialness : False
fixed_value : 1

# NE in alpha games


# Change inner product in Game
metric_type : 'euclidean'
manual_metric_generators : 0

# If payoff_variable_format == 'x' the variables that appear in the parametric solution of the harmonic system are x1, x2, ..., xAN
# Else, the variables are strings like u(i, (a)), explicitely showing player and strategy profile
payoff_variable_format : 'x'

  